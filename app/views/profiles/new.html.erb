<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Update Profile</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f8f9fa;
      padding-top: 50px;
    }

    .card {
      border: none;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .card-header {
      background-color: ;
      color: black;
      border-radius: 10px 10px 0 0;
      padding: 20px 0;
      text-align: center;
    }

    .card-header h2 {
      margin: 0;
      font-size: 24px;
    }

    .card-body {
      padding: 20px;
    }

    .form-group {
      margin-bottom: 20px;
    }

    .form-label {
      font-weight: bold;
      color: #343a40;
    }

    .form-control {
      border-radius: 5px;
      border-color: #ced4da;
    }

    .btn-primary {
      background-color: #007bff;
      border-color: #007bff;
      border-radius: 5px;
      width: 100%;
    }

    .btn-primary:hover {
      background-color: #0056b3;
      border-color: #0056b3;
    }

    .text-danger {
      color: #dc3545;
    }
  </style>
</head>
<body>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card">
        <div class="card-header bg-dark text-light">
          <h2 class="fw-bold">Update Profile</h2>
        </div>
        <div class="card-body">
          <%= form_with(model: @profile, local: true) do |form| %>
            <% if @profile.errors.any? %>
              <div id="error_explanation">
                <h2><%= pluralize(@profile.errors.count, 'error') %> prohibited this profile from being saved:</h2>
                <ul>
                  <% @profile.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
            <% end %>
            <div class="mb-3">
              <%= form.label :name, "User Name <span class='text-danger'>*</span>".html_safe, class: "form-label" %>
              <%= form.text_field :name, class: "form-control", required: true %>
            </div>

             <div class="row mb-3">
              <div class="col-12">
                <%= form.label :gender, "Gender <span class='text-danger'>*</span>".html_safe, class: "form-label" %>
                <%= form.select :gender, [['Male', :male], ['Female', :female]],{}, required: true, class: "form-control" %>
              </div>
            </div>

            <div class="mb-3">
              <%= form.label :contact_number,"Contact Number <span class='text-danger'>*</span>".html_safe, class: "form-label" %>
              <%= form.number_field :contact_number, class: "form-control", required: true, placeholder: 'xxx-xxx-xxxx'  %>
            </div>

            <div class="mb-3">
              <%= form.label :address, "Address <span class='text-danger'>*</span>".html_safe, class: "form-label" %>
              <%= form.text_area :address, class: "form-control", required: true %>
            </div>

            <div class="mb-3 text-center">
              <%= form.submit "Update Profile", class: "btn btn-warning fw-bold" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>

</body>
</html>
