<!DOCTYPE html>
<html>
<head>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: Arial, sans-serif;
      background-color: #F4F4F4;
      text-align: center;
    }

.form-container {
  margin-top: 50px;
}
.card {
  border-radius: 10px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  height:400px; 
}

.card-title {
  color: #333;
}

.form-control {
  border-radius: 8px;
}

.btn-success {
  background-color: #28a745;
  border-color: #28a745;
  margin-bottom: 30px;
}

.btn-success:hover {
  background-color: #218838;
  border-color: #218838;
}
  </style>
</head>

<body>
<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card mt-5">
        <div class="card-body d-flex justify-content-between align-items-center">
          <h3 class="card-title text-left fw-bold mb-0">Update Profile</h3>
          <%= link_to "Back", profiles_path, class: "btn text-primary" %>
        </div>
        <%= form_with model: @profile do |form| %>
          <div class="card-body">
            <div class="mb-3">
              <%= form.text_field :name, placeholder: 'Name', class: "form-control" %>
            </div>
            <div class="mb-3">
              <%= form.select :gender, [['Male', :male], ['Female', :female]], {}, class: "form-select" %>
            </div>
            <div class="mb-3">
              <%= form.number_field :contact_number, placeholder: 'Contact Number', class: "form-control" %>
            </div>
            <div class="mb-3">
              <%= form.text_area :address, placeholder: 'Address', class: "form-control" %>
            </div>
            <%= form.submit "Update Profile", class: "btn btn-success w-100 fw-bold" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
</body>


</body>
</html>







