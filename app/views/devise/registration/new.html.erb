<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  
    <div class="field">
      <%= f.label :role %>
      <%= f.select :role, ['buyer', 'seller'], prompt: 'Select Role' %>
    </div>
  
  
  <% end %>
  