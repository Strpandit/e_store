<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    h1 {
      text-align: center;
      height: 60px;
      line-height: 60px;
    }

    .actions {
      text-align: center;
    }
    .actions a, .actions button {
      margin: 0 5px;
      text-decoration: none;
      color: #007BFF;
      transition: color 0.3s;
    }
    .actions a:hover {
      color: #0056B3;
    }

    button {
      border: none;
      background-color: #C82333;
      color: white;
      padding: 8px 16px;
      text-align: center;
      text-decoration: none;
      display: inline-block;
      font-size: 14px;
      cursor: pointer;
      border-radius: 3px;
      transition: background-color 0.3s;
    }
    button:hover {
      background-color: #971B28;
    }

    
  </style>
</head>
<body style="font-family: 'Mukta', sans-serif;">
<h1 class='bg-warning my-4 py-2 text-light' style="font-weight:bold;">Company Detail</h1>
<div class="container">
<table class="table">
  <tr style="background-color: #F2F2F2;">
    <th scope="col" style="border-bottom: 1px solid #DEE2E6;">Company Name</th>
    <th scope="col" style="border-bottom: 1px solid #DEE2E6;">Company Email</th>
    <th scope="col" style="border-bottom: 1px solid #DEE2E6;">Company Address</th>
    <th scope="col" style="border-bottom: 1px solid #DEE2E6;">Contact Number</th>
    <th scope="col" style="border-bottom: 1px solid #DEE2E6;">Action</th>
  </tr>

  <% if current_user.company_detail.present? %>
    <tr>
      <td class="pt-5"><%= @company_detail.company_name %></td>
      <td class="pt-5"><%= @company_detail.company_email %></td>
      <td class="pt-5"><%=@company_detail.company_phone %></td>
      <td class="pt-5"><%= @company_detail.company_address%></td>
      
      <td>
      
        <%= link_to "Edit",  edit_company_detail_path(@company_detail), class:'btn btn-primary px-4 my-2' %>
        <%= button_to 'Delete', delete_companies_details_path(@company_detail), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, method: :delete, class:'btn btn-danger px-3 my-2' %>
      </td>
    </tr>
  <%end%>
</table>
</div>
</body>
</html>