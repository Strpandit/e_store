<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    table {
      font-family: Arial, sans-serif;
      border-collapse: collapse;
      width: 100%;
      text-align: center;
    }

    th, td {
      
      text-align: center;
      padding: 12px;
    }

    /* tr:nth-child(even) {
      
    } */

    h1 {
      text-align: center;
      height: 60px;
      line-height: 60px;
    }

    button {
      /* width: 150px; */
      border: none;
      background-color: #3498db;
      /* color: white; */
      padding: 8px 16px;
      text-align: center;
      text-decoration: none;
      display: inline-block;
      font-size: 14px;
      cursor: pointer;
      /* border-radius: 4px; */
    }

    button:hover {
      /* background-color: #2980b9; */
    }


  </style>
</head>
<body>

<h1>Company List</h1>

<table>

    

  <tr>
    <th scope="col">Company Name</th>
    <th scope="col">Company Email</th>
    <th scope="col">Company Address</th>
    <th scope="col">Contact Number</th>
    <th>Action</th>
  </tr>

  <% if current_user.company_detail.present? %>
    <tr>
      <td><%= @company_detail.company_name %></td>
      <td><%= @company_detail.company_email %></td>
       <td><%=@company_detail.company_phone %></td>
      <td><%= @company_detail.company_address%></td>
      
      <td>
      
        <%= link_to "Edit",  edit_company_detail_path(@company_detail) %>
                <%= button_to 'Delete', delete_companies_details_path(@company_detail), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, method: :delete %>
      </td>
    </tr>
  <%end%>
</table>

</body>
</html>


















